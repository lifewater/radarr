---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-radarr-config
  namespace: homelab
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  volumeName: pv-radarr-config
  storageClassName: nfs-csi
